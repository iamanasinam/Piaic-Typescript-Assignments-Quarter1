export default function findingAge (brithYear: number, yearNow: number){
    let age :number = yearNow - brithYear;
    console.log(`\nQ1: Finding age based on the current year.\nI'm ${age} years old.`)}